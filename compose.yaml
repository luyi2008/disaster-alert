name: disaster-alert

services:
  disaster-alert:
    image: ${DISASTER_ALERT_IMAGE:-ghcr.io/luyi2008/disaster-alert:latest}
    build:
      context: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: ${SERVER_PORT:-30010}
      DB_PATH: /data/disaster-alert.fjall
    ports:
      - target: ${SERVER_PORT:-30010}
        published: ${SERVER_PORT:-30010}
        host_ip: ${SERVER_PUBLISH_HOST:-0.0.0.0}
        protocol: tcp
    volumes:
      - disaster-alert-data:/data
    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - --silent
        - --show-error
        - http://127.0.0.1:${SERVER_PORT:-30010}/health
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    # Shutdown can use three sequential timeout windows plus forced cleanup.
    stop_grace_period: 16m

volumes:
  disaster-alert-data:
